import { Component } from '@angular/core';

@Component({
    selector: 'home',
    //template: '<div *ngFor="let p of paises"> {{p.name}}</div> ',
    templateUrl: '/app/home/home.component.template.html',
    //providers: [InicioService],     
    //pipes: [InicioPipe], 
    
})
export class home {
    
}